name: .NET Core

on:
  push:
    branches: [ main ]
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    permissions:
      actions: read
      contents: write
      deployments: read
      pages: write
      id-token: write
    uses: dpvreony/.github/.github/workflows/dotnet-ci.yml@460a1ec64f9764b424388a214cbf6d1d407ef07d
    with:
      solutionName: "Dhgms.DocFx.MermaidJs"

