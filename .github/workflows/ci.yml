name: .NET Core

on:
  push:
    branches: [ main ]
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    permissions:
      actions: read
      contents: write
      deployments: read
      pages: write
      id-token: write
    uses: dpvreony/.github/.github/workflows/dotnet-ci.yml@9d34d23854a35cb109a619fe54ea6503d5672697
    with:
      solutionName: "Dhgms.DocFx.MermaidJs"
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}

